- hosts: localhost

  tasks:
    - name: Set favorite apps
      command: >
        gsettings set org.gnome.shell favorite-apps
        "[\"org.gnome.Terminal.desktop\", \"google-chrome.desktop\", \"webstorm.desktop\", \"phpstorm.desktop\"]"
